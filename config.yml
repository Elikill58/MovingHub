debug: false

detect-shutdown:
- "shutdown"
- "restart"
- "closed"

groups:
  ignored:
    ignore_case: true
    names:
    - "Connect-01"
    contains:
    - "connect"
  redirect:
    ignore_case: true
    names:
    - "Lobby-01"
    contains:
    - "lobby"
